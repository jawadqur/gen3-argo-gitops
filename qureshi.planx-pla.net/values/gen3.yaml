global:
  hostname: qureshi.planx-pla.net
  dev: false
  postgres: 
    master:
      host: gen3-postgresql
      user: postgres
      password: test

revproxy:
  ingress:
    className: kong
    enabled: true
    hosts:
    # Replace with your hostname
    - host: qureshi.planx-pla.net
      paths:
        - path: /
          pathType: Prefix
    tls:
      # this is the secret generated by the cert and key from global.tls
      # if you have your own secret, reference that. 
      - secretName: gen3-certs
        hosts:
        # Replace with your hostname
          - qureshi.planx-pla.net

  

arborist:
  postgres:
    # Helm lookup doesn't work in ArgoCD
    password: test

audit: 
  postgres:
    # Helm lookup doesn't work in ArgoCD
    password: test

indexd:
  postgres:
    # Helm lookup doesn't work in ArgoCD
    password: test

sheepdog:
  postgres:
    # Helm lookup doesn't work in ArgoCD
    password: test

